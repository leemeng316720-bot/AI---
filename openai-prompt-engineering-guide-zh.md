# OpenAI 提示词工程指南（精炼版）

> **来源：** [OpenAI 官方文档](https://developers.openai.com/api/docs/guides/prompt-engineering)  
> **更新时间：** 2026 年（基于 GPT-5.6 与 Responses API）

---

## 1. 核心概念

### 什么是提示词工程？
提示词工程（Prompt Engineering）是为模型编写有效指令的过程，使其能够持续生成符合要求的内容。它既是艺术，也是科学。

### 模型选择
| 模型类型 | 最佳场景 | 特点 |
|---|---|---|
| **推理模型** (o3, o4-mini) | 复杂任务、多步规划 | 内部思维链；较慢且更昂贵 |
| **GPT 模型** (gpt-5.6, gpt-4.1) | 快速、高性价比生成 | 需要更明确的指令 |
| **大模型** | 跨领域问题解决 | 理解能力更强，成本更高 |
| **小模型** (mini/nano) | 高吞吐量任务 | 更快、更便宜 |

> 默认推荐：`gpt-5.6` 用于通用文本生成。

---

## 2. 消息角色与权限层级

使用 `instructions` 参数（或 `developer` 角色）设定高层行为规则；使用 `user` 消息传递具体任务输入。

| 角色 | 优先级 | 用途 |
|---|---|---|
| `developer` | 最高 | 系统规则、语气、目标、业务逻辑 |
| `user` | 较低 | 终端用户输入、具体任务细节 |
| `assistant` | — | 模型生成的回复 |

**类比：** `developer` 消息 = 函数定义；`user` 消息 = 函数参数。

---

## 3. 提示词结构（Markdown + XML）

一条结构良好的 `developer` 消息通常按以下顺序组织：

```markdown
# 身份（Identity）
描述助手的目的、沟通风格和高层目标。

# 指令（Instructions）
- 模型必须遵守的规则
- 应该做什么 / 绝不能做什么
- 工具使用规范

# 示例（Examples）
<user_query>...</user_query>
<assistant_response>...</assistant_response>

# 上下文（Context）
额外的专有数据或相关信息（放在提示词末尾）。
```

**技巧：**
- 使用 **Markdown 标题/列表** 构建层级，提升可读性。
- 使用 **XML 标签** 划分内容边界（如 `<document>`、`<user_query>`）。
- 将可复用的稳定内容放在提示词**开头**，以最大化 Prompt Caching 的节省效果。

---

## 4. 少样本学习（Few-Shot Learning）

通过在 `developer` 消息中嵌入 2–5 组多样化的输入/输出示例，引导模型掌握新任务。模型会从示例中隐式学习模式并应用到新输入。

**最佳实践：**
- 展示**多样化**的输入（常见场景 + 边界情况）。
- 示例的**质量**应与预期任务复杂度相匹配。
- 使用 XML `id` 属性清晰配对示例。

---

## 5. 上下文与 RAG

提供额外相关上下文的目的：
- 让模型基于专有数据作答
- 将回答约束在特定资源范围内

**技术方案：**
- 向量数据库检索 → 将文本注入提示词
- OpenAI 内置的文件搜索工具
- 使用 XML 标签直接包含文档

**上下文窗口规划：** 了解模型的 Token 上限。GPT-4.1 支持最高 100 万 Token；小模型上限更低。

---

## 6. GPT-5 系列提示词技巧

### 编程任务
- **显式定义角色**（如"软件工程智能体"）。
- **强制结构化工具使用**，附具体示例。
- **要求测试** — 指示模型在修改后运行单元测试或验证。
- **设定 Markdown 标准**，确保输出整洁（路径/函数名用反引号）。

### 前端开发
在提示词中包含以下类别指令：
1. **原则** — 视觉质量标准、模块化组件、设计一致性
2. **UI/UX** — 字体、配色、间距/布局、交互状态（悬停、空态、加载）、无障碍
3. **结构** — 文件/文件夹布局定义
4. **组件** — 可复用包装示例、前后端分离策略
5. **页面** — 常见布局模板
6. **智能体指令** — 确认设计假设、搭建脚手架、强制执行标准、集成 API、测试状态、编写文档

### 智能体工作流（长时运行任务）
1. **充分规划** — 行动前将任务分解为子任务。
2. **透明序言** — 在关键步骤解释调用工具的原因。
3. **TODO / 评分表追踪** — 强制结构化规划，避免遗漏步骤。
4. **持久性指令：**
   > "持续工作直到用户的问题完全解决。不要在只完成部分请求后就停止。"

---

## 7. 推理模型（o3 / o4-mini）

| | 推理模型 | GPT 模型 |
|---|---|---|
| **比喻** | 资深同事 | 初级同事 |
| **提示风格** | 高层目标，细节交给模型 | 显式分步指令 |
| **最佳场景** | 数学、逻辑、深度分析 | 创意、快速、结构化输出 |

**核心洞察：** 推理模型在指令*不那么精确*时表现更好 —— 给它们目标，让它们自己推导细节。

---

## 8. 生产环境最佳实践

### 在代码中版本化提示词
OpenAI 正在弃用可复用的 Prompt Objects（`v1/prompts` 将于 2026 年 11 月 30 日关闭）。**请将提示词存储在代码中：**
- 命名模块（如 `prompts/supportReply.ts`）
- 对动态值使用类型化函数参数
- 直接将 `instructions` + `input` 传入 Responses API
- 变更需配套测试、夹具和评估检查
- 使用 Git/PR/功能标志进行发布

### Prompt Caching（提示词缓存）
将稳定的前缀内容放在提示词**开头**，以及 JSON 请求体的**最前面**，以降低缓存命中时的延迟和成本。

### 系统化测试
- 用 5–10 个多样化输入测试提示词（简单、中等、边界情况）。
- 记录输出并标记格式/语气失败项。
- 迭代时**每次只改变一个变量**。
- 早期发现问题比在生产环境修复便宜得多。

---

## 9. 六大经典策略（基础框架）

这些策略对所有模型仍然适用：

1. **编写清晰指令** — 明确格式、长度、受众、语气、排除项。
2. **提供参考文本** — 基于源材料作答，减少幻觉。
3. **拆分复杂任务** — 使用连续的子提示词；每步验证。
4. **给模型"思考"时间** — 思维链（"请逐步思考后再回答"）。
5. **使用外部工具** — 代码解释器、网页搜索、函数调用。
6. **系统化测试变更** — 在代表性样本上评估改动效果。

---

## 10. 快速检查清单

发送提示词前，请确认：
- [ ] 为任务选择了合适的模型
- [ ] 在 `developer` 消息中定义了角色/身份
- [ ] 指令明确且每条只陈述一次
- [ ] 包含少样本示例（如任务较复杂）
- [ ] 上下文已注入（放在末尾）
- [ ] 使用 XML 标签划分内容边界
- [ ] 已指定输出格式
- [ ] 已准备测试用例

---

*本文档是对 OpenAI 官方提示词工程文档的精炼整合。*
